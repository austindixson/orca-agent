# README visual generator (Remotion)

This project renders the PNG visuals embedded in the root README.

Outputs:
- `../assets/install-flow.png`
- `../assets/setup-flow.png`

Commands:

```bash
npm install
npm run compositions
npm run render:all
```

Single render:

```bash
npm run render:install
npm run render:setup
```
